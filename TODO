
* Handle comments in configuration files when writing them back.

- Possibly requires implementing a special MySQL configuration file
   handler

* Have tests use mock database by default and allow tests to connect
   to real database as an option

* Add roles from old repository

- Add tests for the roles.

* Add examples from old repository

* Add remote binlog reader from old repository

* Add remote binlog parser from old repository

